const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "pushcartusername",
      password: "pushcartpassword",
      database: "pushcart",
    },
    listPerPage: 500,
  };
  module.exports = config;